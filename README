These collections of sample code are focused on the fundamentals of working with the ReachMail API. For the most part, they deal with accessing a specific service and are not complex programs.

The Python section includes a file called rmApiLib.py which is a small Python library that covers most of the basic API services.
